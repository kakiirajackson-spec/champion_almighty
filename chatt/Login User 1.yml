info:
  name: Login User 1
  type: http
  seq: 2

http:
  method: POST
  url: https://champion-almighty.onrender.com/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "almighty@test.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
