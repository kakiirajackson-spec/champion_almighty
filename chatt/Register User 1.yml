info:
  name: Register User 1
  type: http
  seq: 1

http:
  method: POST
  url: https://champion-almighty.onrender.com/api/auth/register
  body:
    type: json
    data: |-
      {
        "username": "champion",
        "email": "champion@test.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
